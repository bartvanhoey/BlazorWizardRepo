<CascadingValue Value="this">
    <div id="@Id">
        @* <ul class="nav nav-pills nav-justified"> *@
        @*     @foreach (var step in _steps) *@
        @*     { *@
        @*         <li id="step-@(StepsIndex(step) + 1)" class="nav-item"> *@
        @*             <a class="nav-link @((ActiveStep == step) ? "active" : "")" href="javascript: void(0)" *@
        @*                @onclick="@(e=> SetActive(step))">@step.Name</a> *@
        @*         </li> *@
        @*     } *@
        @* </ul> *@
        <div id="container-fluid">
            @ChildContent
        </div>
    </div>
    <button class="btn btn-primary" type="button" disabled="@(ActiveStepIx == 0)" @onclick="PreviousButtonClicked">Previous </button>


    @if (ShowNextButton)
    {
        <button class="btn btn-primary"  type="@(IsLastStep ? "submit" : "button")" @onclick="NextButtonClicked">@(IsLastStep ? "Submit" : "Next")</button>
    }


</CascadingValue>
